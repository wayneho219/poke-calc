# i18n Trilingual UI Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add Traditional Chinese / English / Japanese UI language switching to the Streamlit app, with browser language auto-detection and URL-based persistence.

**Architecture:** A `Translator` class in `shared/i18n/` loads one of three JSON files at runtime and provides `t("key")` string lookups. A pure `parse_accept_language()` function handles browser language detection. `app.py` detects language on each page load (URL param → browser header → fallback zh), renders a sidebar selector, and replaces all hardcoded strings with `t("key")` calls.

**Tech Stack:** Python 3.11+, Streamlit 1.37+, stdlib only (json, pathlib)

---

## File Map

| File | Action | Responsibility |
|---|---|---|
| `shared/config.py` | Modify | Add `I18N_DIR` path constant |
| `shared/i18n/__init__.py` | Create | Package marker |
| `shared/i18n/translator.py` | Create | `Translator` class, `parse_accept_language()` |
| `shared/i18n/zh.json` | Create | Traditional Chinese strings (50 keys + 18 types) |
| `shared/i18n/en.json` | Create | English strings |
| `shared/i18n/ja.json` | Create | Japanese strings |
| `tests/shared/__init__.py` | Create | Package marker |
| `tests/shared/test_translator.py` | Create | Unit tests for Translator |
| `tests/shared/test_lang_detection.py` | Create | Unit tests for parse_accept_language |
| `interfaces/streamlit/app.py` | Modify | Lang detection, sidebar, replace all strings |

---

## Task 1: Config + Directory Scaffold

**Files:**
- Modify: `shared/config.py`
- Create: `shared/i18n/__init__.py`
- Create: `tests/shared/__init__.py`

- [ ] **Step 1: Add `I18N_DIR` to `shared/config.py`**

Current content:
```python
from pathlib import Path

ROOT      = Path(__file__).parent.parent
CSV_PATH  = ROOT / "data" / "pokemon_names.csv"
CACHE_DIR = ROOT / "adapters" / "cache"
```

Replace with:
```python
from pathlib import Path

ROOT      = Path(__file__).parent.parent
CSV_PATH  = ROOT / "data" / "pokemon_names.csv"
CACHE_DIR = ROOT / "adapters" / "cache"
I18N_DIR  = ROOT / "shared" / "i18n"
```

- [ ] **Step 2: Create package markers**

```bash
mkdir -p shared/i18n tests/shared
touch shared/i18n/__init__.py
touch tests/shared/__init__.py
```

- [ ] **Step 3: Smoke-test the import**

```bash
python3 -c "from shared.config import I18N_DIR; print(I18N_DIR)"
```

Expected: prints `/Users/wayneho/poke-calc/shared/i18n`

- [ ] **Step 4: Commit**

```bash
git add shared/config.py shared/i18n/__init__.py tests/shared/__init__.py
git commit -m "feat(i18n): add I18N_DIR config and i18n package scaffold"
```

---

## Task 2: Translator Class + Tests (TDD)

**Files:**
- Create: `tests/shared/test_translator.py`
- Create: `tests/shared/test_lang_detection.py`
- Create: `shared/i18n/translator.py`

> Note: The JSON files don't exist yet. We create minimal fixture JSON for tests; the real files come in Task 3.

- [ ] **Step 1: Write failing tests for `Translator`**

Create `tests/shared/test_translator.py`:

```python
import json
import pytest
from pathlib import Path
from shared.i18n.translator import Translator, parse_accept_language


# ── Fixtures ─────────────────────────────────────────────────────────────────

@pytest.fixture
def tmp_i18n_dir(tmp_path):
    en = {
        "greeting": "Hello",
        "stat_names": ["HP", "Attack"],
        "types": {"fire": "Fire", "water": "Water"},
    }
    zh = {
        "greeting": "你好",
        "stat_names": ["HP", "攻擊"],
        "types": {"fire": "火", "water": "水"},
    }
    ja = {
        "greeting": "こんにちは",
        "stat_names": ["HP", "こうげき"],
        "types": {"fire": "ほのお", "water": "みず"},
    }
    for lang, data in [("en", en), ("zh", zh), ("ja", ja)]:
        (tmp_path / f"{lang}.json").write_text(
            json.dumps(data, ensure_ascii=False), encoding="utf-8"
        )
    return tmp_path


# ── Translator tests ──────────────────────────────────────────────────────────

class TestTranslator:
    def test_en_key_returns_english(self, tmp_i18n_dir):
        t = Translator("en", i18n_dir=tmp_i18n_dir)
        assert t("greeting") == "Hello"

    def test_zh_key_returns_chinese(self, tmp_i18n_dir):
        t = Translator("zh", i18n_dir=tmp_i18n_dir)
        assert t("greeting") == "你好"

    def test_ja_key_returns_japanese(self, tmp_i18n_dir):
        t = Translator("ja", i18n_dir=tmp_i18n_dir)
        assert t("greeting") == "こんにちは"

    def test_missing_key_falls_back_to_key_name(self, tmp_i18n_dir):
        t = Translator("en", i18n_dir=tmp_i18n_dir)
        assert t("nonexistent_xyz") == "nonexistent_xyz"

    def test_strings_returns_list(self, tmp_i18n_dir):
        t = Translator("en", i18n_dir=tmp_i18n_dir)
        assert t.strings("stat_names") == ["HP", "Attack"]

    def test_strings_missing_key_returns_empty_list(self, tmp_i18n_dir):
        t = Translator("en", i18n_dir=tmp_i18n_dir)
        assert t.strings("no_such_list") == []

    def test_type_name_en(self, tmp_i18n_dir):
        t = Translator("en", i18n_dir=tmp_i18n_dir)
        assert t.type_name("fire") == "Fire"

    def test_type_name_zh(self, tmp_i18n_dir):
        t = Translator("zh", i18n_dir=tmp_i18n_dir)
        assert t.type_name("fire") == "火"

    def test_type_name_ja(self, tmp_i18n_dir):
        t = Translator("ja", i18n_dir=tmp_i18n_dir)
        assert t.type_name("fire") == "ほのお"

    def test_type_name_unknown_falls_back(self, tmp_i18n_dir):
        t = Translator("en", i18n_dir=tmp_i18n_dir)
        assert t.type_name("shadow") == "shadow"
```

Create `tests/shared/test_lang_detection.py`:

```python
from shared.i18n.translator import parse_accept_language


class TestParseAcceptLanguage:
    def test_zh_tw_returns_zh(self):
        assert parse_accept_language("zh-TW,zh;q=0.9,en;q=0.8") == "zh"

    def test_zh_hans_returns_zh(self):
        assert parse_accept_language("zh-Hans") == "zh"

    def test_zh_bare_returns_zh(self):
        assert parse_accept_language("zh") == "zh"

    def test_ja_returns_ja(self):
        assert parse_accept_language("ja,en-US;q=0.9") == "ja"

    def test_ja_jp_returns_ja(self):
        assert parse_accept_language("ja-JP") == "ja"

    def test_english_returns_zh(self):
        assert parse_accept_language("en-US,en;q=0.9") == "zh"

    def test_korean_returns_zh(self):
        assert parse_accept_language("ko-KR,ko;q=0.9") == "zh"

    def test_empty_header_returns_zh(self):
        assert parse_accept_language("") == "zh"
```

- [ ] **Step 2: Run tests to verify they fail**

```bash
python3 -m pytest tests/shared/ -v
```

Expected: `ModuleNotFoundError: No module named 'shared.i18n.translator'`

- [ ] **Step 3: Implement `shared/i18n/translator.py`**

Create `shared/i18n/translator.py`:

```python
from __future__ import annotations
import json
from pathlib import Path
from shared.config import I18N_DIR


def parse_accept_language(header: str) -> str:
    if not header:
        return "zh"
    first = header.split(",")[0].split(";")[0].strip().lower()
    if first.startswith("zh"):
        return "zh"
    if first.startswith("ja"):
        return "ja"
    return "zh"


class Translator:

    def __init__(self, lang: str, i18n_dir: Path = I18N_DIR) -> None:
        path = i18n_dir / f"{lang}.json"
        self._data: dict = json.loads(path.read_text(encoding="utf-8"))

    def __call__(self, key: str) -> str:
        value = self._data.get(key, key)
        return value if isinstance(value, str) else key

    def strings(self, key: str) -> list[str]:
        value = self._data.get(key)
        return value if isinstance(value, list) else []

    def type_name(self, type_en: str) -> str:
        return self._data.get("types", {}).get(type_en, type_en)
```

- [ ] **Step 4: Run tests to verify they pass**

```bash
python3 -m pytest tests/shared/ -v
```

Expected: All 18 tests PASS.

- [ ] **Step 5: Run full test suite to verify no regressions**

```bash
python3 -m pytest -q
```

Expected: All 79 tests pass (61 existing + 18 new).

- [ ] **Step 6: Commit**

```bash
git add shared/i18n/translator.py tests/shared/test_translator.py tests/shared/test_lang_detection.py
git commit -m "feat(i18n): add Translator class and parse_accept_language with tests"
```

---

## Task 3: JSON Translation Files

**Files:**
- Create: `shared/i18n/zh.json`
- Create: `shared/i18n/en.json`
- Create: `shared/i18n/ja.json`

- [ ] **Step 1: Create `shared/i18n/zh.json`**

```json
{
  "page_title": "Pokémon Champions 2026 — 競技策略工具箱",
  "tab_search": "🔍 寶可夢查詢",
  "tab_speed": "⚡ 超速分析",
  "tab_survival": "🛡️ 存活分析",
  "search_header": "🔍 寶可夢查詢",
  "search_input_label": "輸入名稱（繁中 / English / 日本語）",
  "search_placeholder": "烈咬陸鯊 / Garchomp / ガブリアス",
  "search_spinner": "搜尋中...",
  "search_not_found": "找不到符合的寶可夢，請確認名稱是否正確。",
  "search_types_label": "屬性：",
  "stat_col_name": "能力",
  "stat_col_value": "種族值",
  "stat_names": ["HP", "攻擊", "防禦", "特攻", "特防", "速度"],
  "speed_header": "⚡ 超速分析",
  "speed_caption": "計算超越目標對手（速度+1）所需的最小 SP_Speed 分配。目標寶可夢假設 SP_Speed = 0。",
  "speed_my_mon": "我方寶可夢",
  "speed_tgt_mon": "目標寶可夢",
  "speed_name_label": "名稱",
  "speed_nature_label": "性格",
  "speed_nature_other": "其他...",
  "speed_nature_input": "輸入性格名稱（中/英/日）",
  "speed_button": "計算超速 SP",
  "speed_spinner": "搜尋中...",
  "speed_my_not_found": "找不到我方寶可夢：{name}",
  "speed_tgt_not_found": "找不到目標寶可夢：{name}",
  "speed_cannot_outspeed": "❌ 即使投入所有 SP，{my} 仍無法超越 {tgt}（速度差距過大）。",
  "speed_success": "✅ 需要 **SP_Speed = {sp}** 點",
  "speed_metric_sp": "所需 SP",
  "speed_metric_speed": "{name} 速度",
  "surv_header": "🛡️ 存活分析",
  "surv_caption": "找出能扛下特定攻擊的最小 SP_HP + SP_Def 總和，同時呈現偏HP與偏防禦兩種最優方案。",
  "surv_my_mon": "我方寶可夢",
  "surv_atk_params": "攻擊參數",
  "surv_name_label": "名稱",
  "surv_nature_label": "性格",
  "surv_nature_other": "其他...",
  "surv_nature_input": "輸入性格名稱（中/英/日）",
  "surv_power_label": "招式威力",
  "surv_atk_label": "攻擊方實際攻擊力",
  "surv_cat_label": "攻擊類別",
  "surv_cat_physical": "物理",
  "surv_cat_special": "特殊",
  "surv_mult_label": "屬性相性",
  "surv_button": "計算最佳存活分配",
  "surv_spinner": "搜尋中...",
  "surv_not_found": "找不到寶可夢：{name}",
  "surv_impossible": "❌ 在 SP 限制內無法扛下此攻擊，請調整攻擊參數。",
  "surv_success": "✅ 最小 SP 總投入：**{sp}** 點（SP_HP + SP_Def）",
  "surv_plan_hp": "偏 HP 方案",
  "surv_plan_def": "偏防禦方案",
  "surv_metric_sp_hp": "SP_HP",
  "surv_metric_sp_def": "SP_Def",
  "surv_metric_final_hp": "最終 HP",
  "surv_metric_final_def": "最終防禦",
  "surv_metric_total": "合計 SP 投入：{sp}",
  "nature_invalid": "無法識別的性格：{error}",
  "types": {
    "normal": "一般", "fire": "火", "water": "水",
    "electric": "電", "grass": "草", "ice": "冰",
    "fighting": "格鬥", "poison": "毒", "ground": "地面",
    "flying": "飛行", "psychic": "超能力", "bug": "蟲",
    "rock": "岩石", "ghost": "幽靈", "dragon": "龍",
    "dark": "惡", "steel": "鋼", "fairy": "妖精"
  }
}
```

- [ ] **Step 2: Create `shared/i18n/en.json`**

```json
{
  "page_title": "Pokémon Champions 2026 — Competitive Toolbox",
  "tab_search": "🔍 Pokémon Search",
  "tab_speed": "⚡ Speed Analysis",
  "tab_survival": "🛡️ Survival Analysis",
  "search_header": "🔍 Pokémon Search",
  "search_input_label": "Enter name (繁中 / English / 日本語)",
  "search_placeholder": "Garchomp / 烈咬陸鯊 / ガブリアス",
  "search_spinner": "Searching...",
  "search_not_found": "Pokémon not found. Please check the name.",
  "search_types_label": "Types: ",
  "stat_col_name": "Stat",
  "stat_col_value": "Base",
  "stat_names": ["HP", "Attack", "Defense", "Sp. Atk", "Sp. Def", "Speed"],
  "speed_header": "⚡ Speed Analysis",
  "speed_caption": "Find the minimum SP_Speed needed to outspeed the target (target assumed SP = 0).",
  "speed_my_mon": "My Pokémon",
  "speed_tgt_mon": "Target Pokémon",
  "speed_name_label": "Name",
  "speed_nature_label": "Nature",
  "speed_nature_other": "Other...",
  "speed_nature_input": "Enter nature name (zh/en/ja)",
  "speed_button": "Calculate Speed SP",
  "speed_spinner": "Searching...",
  "speed_my_not_found": "My Pokémon not found: {name}",
  "speed_tgt_not_found": "Target Pokémon not found: {name}",
  "speed_cannot_outspeed": "❌ Even with max SP, {my} cannot outspeed {tgt}.",
  "speed_success": "✅ SP_Speed needed: **{sp}**",
  "speed_metric_sp": "SP needed",
  "speed_metric_speed": "{name} Speed",
  "surv_header": "🛡️ Survival Analysis",
  "surv_caption": "Find the minimum SP_HP + SP_Def to survive a hit, showing both HP-biased and Defense-biased optimal plans.",
  "surv_my_mon": "My Pokémon",
  "surv_atk_params": "Attack Parameters",
  "surv_name_label": "Name",
  "surv_nature_label": "Nature",
  "surv_nature_other": "Other...",
  "surv_nature_input": "Enter nature name (zh/en/ja)",
  "surv_power_label": "Move Power",
  "surv_atk_label": "Attacker's Attack Stat",
  "surv_cat_label": "Category",
  "surv_cat_physical": "Physical",
  "surv_cat_special": "Special",
  "surv_mult_label": "Type Effectiveness",
  "surv_button": "Calculate Optimal SP",
  "surv_spinner": "Searching...",
  "surv_not_found": "Pokémon not found: {name}",
  "surv_impossible": "❌ Cannot survive this hit within SP limits. Adjust the attack parameters.",
  "surv_success": "✅ Minimum total SP: **{sp}** (SP_HP + SP_Def)",
  "surv_plan_hp": "HP-biased Plan",
  "surv_plan_def": "Defense-biased Plan",
  "surv_metric_sp_hp": "SP_HP",
  "surv_metric_sp_def": "SP_Def",
  "surv_metric_final_hp": "Final HP",
  "surv_metric_final_def": "Final Def",
  "surv_metric_total": "Total SP: {sp}",
  "nature_invalid": "Unrecognised nature: {error}",
  "types": {
    "normal": "Normal", "fire": "Fire", "water": "Water",
    "electric": "Electric", "grass": "Grass", "ice": "Ice",
    "fighting": "Fighting", "poison": "Poison", "ground": "Ground",
    "flying": "Flying", "psychic": "Psychic", "bug": "Bug",
    "rock": "Rock", "ghost": "Ghost", "dragon": "Dragon",
    "dark": "Dark", "steel": "Steel", "fairy": "Fairy"
  }
}
```

- [ ] **Step 3: Create `shared/i18n/ja.json`**

```json
{
  "page_title": "Pokémon Champions 2026 — 競技戦略ツールボックス",
  "tab_search": "🔍 ポケモン検索",
  "tab_speed": "⚡ すばやさ分析",
  "tab_survival": "🛡️ 耐久分析",
  "search_header": "🔍 ポケモン検索",
  "search_input_label": "名前を入力（繁中 / English / 日本語）",
  "search_placeholder": "ガブリアス / Garchomp / 烈咬陸鯊",
  "search_spinner": "検索中...",
  "search_not_found": "ポケモンが見つかりません。名前を確認してください。",
  "search_types_label": "タイプ：",
  "stat_col_name": "ステータス",
  "stat_col_value": "種族値",
  "stat_names": ["HP", "こうげき", "ぼうぎょ", "とくこう", "とくぼう", "すばやさ"],
  "speed_header": "⚡ すばやさ分析",
  "speed_caption": "相手より速くなるために必要な最小 SP_Speed を計算します（相手の SP = 0 を仮定）。",
  "speed_my_mon": "自分のポケモン",
  "speed_tgt_mon": "目標ポケモン",
  "speed_name_label": "名前",
  "speed_nature_label": "せいかく",
  "speed_nature_other": "その他...",
  "speed_nature_input": "せいかく名を入力（中/英/日）",
  "speed_button": "すばやさ SP を計算",
  "speed_spinner": "検索中...",
  "speed_my_not_found": "自分のポケモンが見つかりません：{name}",
  "speed_tgt_not_found": "目標ポケモンが見つかりません：{name}",
  "speed_cannot_outspeed": "❌ SP を最大投入しても、{my} は {tgt} を抜けません。",
  "speed_success": "✅ 必要 SP_Speed：**{sp}**",
  "speed_metric_sp": "必要 SP",
  "speed_metric_speed": "{name} すばやさ",
  "surv_header": "🛡️ 耐久分析",
  "surv_caption": "特定の攻撃を耐えるための最小 SP_HP + SP_Def を求め、HP 重視・防御重視の2案を表示します。",
  "surv_my_mon": "自分のポケモン",
  "surv_atk_params": "攻撃パラメータ",
  "surv_name_label": "名前",
  "surv_nature_label": "せいかく",
  "surv_nature_other": "その他...",
  "surv_nature_input": "せいかく名を入力（中/英/日）",
  "surv_power_label": "技の威力",
  "surv_atk_label": "攻撃側の実数値",
  "surv_cat_label": "分類",
  "surv_cat_physical": "物理",
  "surv_cat_special": "特殊",
  "surv_mult_label": "タイプ相性",
  "surv_button": "最適耐久配分を計算",
  "surv_spinner": "検索中...",
  "surv_not_found": "ポケモンが見つかりません：{name}",
  "surv_impossible": "❌ SP 上限内ではこの攻撃を耐えられません。攻撃パラメータを調整してください。",
  "surv_success": "✅ 最小 SP 合計：**{sp}**（SP_HP + SP_Def）",
  "surv_plan_hp": "HP 重視プラン",
  "surv_plan_def": "防御重視プラン",
  "surv_metric_sp_hp": "SP_HP",
  "surv_metric_sp_def": "SP_Def",
  "surv_metric_final_hp": "実数 HP",
  "surv_metric_final_def": "実数ぼうぎょ",
  "surv_metric_total": "合計 SP：{sp}",
  "nature_invalid": "不明なせいかく：{error}",
  "types": {
    "normal": "ノーマル", "fire": "ほのお", "water": "みず",
    "electric": "でんき", "grass": "くさ", "ice": "こおり",
    "fighting": "かくとう", "poison": "どく", "ground": "じめん",
    "flying": "ひこう", "psychic": "エスパー", "bug": "むし",
    "rock": "いわ", "ghost": "ゴースト", "dragon": "ドラゴン",
    "dark": "あく", "steel": "はがね", "fairy": "フェアリー"
  }
}
```

- [ ] **Step 4: Verify Translator loads real JSON files**

```bash
python3 -c "
from shared.i18n.translator import Translator
zh = Translator('zh')
en = Translator('en')
ja = Translator('ja')
print(zh('tab_search'))
print(en('tab_search'))
print(ja('tab_search'))
print(zh.type_name('dragon'))
print(en.type_name('dragon'))
print(ja.type_name('dragon'))
"
```

Expected:
```
🔍 寶可夢查詢
🔍 Pokémon Search
🔍 ポケモン検索
龍
Dragon
ドラゴン
```

- [ ] **Step 5: Run full test suite**

```bash
python3 -m pytest -q
```

Expected: All 79 tests pass.

- [ ] **Step 6: Commit**

```bash
git add shared/i18n/zh.json shared/i18n/en.json shared/i18n/ja.json
git commit -m "feat(i18n): add zh/en/ja translation JSON files"
```

---

## Task 4: Language Detection + Sidebar in `app.py`

**Files:**
- Modify: `interfaces/streamlit/app.py`

Add language detection and sidebar. All existing UI strings stay Chinese for now — this task only wires up the plumbing.

- [ ] **Step 1: Add imports and `detect_lang()` at the top of `app.py`**

Replace the current import block:

```python
import dataclasses
import streamlit as st
from adapters.poke_api_repository import PokeApiRepository
from adapters.csv_name_provider import CsvNameProvider
from application.calculator import StatCalculator
from application.search_service import SearchService
from application.speed_service import SpeedService
from application.survival_service import AttackInput, SurvivalService
from domain.models.nature import NatureRegistry
from shared.config import CSV_PATH, CACHE_DIR
```

With:

```python
import dataclasses
import streamlit as st
from adapters.poke_api_repository import PokeApiRepository
from adapters.csv_name_provider import CsvNameProvider
from application.calculator import StatCalculator
from application.search_service import SearchService
from application.speed_service import SpeedService
from application.survival_service import AttackInput, SurvivalService
from domain.models.nature import NatureRegistry
from shared.config import CSV_PATH, CACHE_DIR
from shared.i18n.translator import Translator, parse_accept_language


def detect_lang() -> str:
    if "lang" in st.query_params:
        candidate = st.query_params["lang"]
        if candidate in ("zh", "en", "ja"):
            return candidate
    try:
        header = st.context.headers.get("Accept-Language", "")
        return parse_accept_language(header)
    except AttributeError:
        return "zh"


lang = detect_lang()
t = Translator(lang)
```

- [ ] **Step 2: Update `st.set_page_config` and add sidebar**

Replace:

```python
st.set_page_config(page_title="Pokémon Champions 2026", layout="wide")
```

With:

```python
st.set_page_config(page_title=t("page_title"), layout="wide")

with st.sidebar:
    chosen = st.selectbox(
        "🌐 Language",
        options=["zh", "en", "ja"],
        format_func=lambda l: {"zh": "繁體中文", "en": "English", "ja": "日本語"}[l],
        index=["zh", "en", "ja"].index(lang),
    )
    if chosen != lang:
        st.query_params["lang"] = chosen
        st.rerun()
```

- [ ] **Step 3: Verify syntax**

```bash
python3 -c "import ast; ast.parse(open('interfaces/streamlit/app.py').read()); print('syntax OK')"
```

Expected: `syntax OK`

- [ ] **Step 4: Run full test suite**

```bash
python3 -m pytest -q
```

Expected: All 79 tests pass.

- [ ] **Step 5: Commit**

```bash
git add interfaces/streamlit/app.py
git commit -m "feat(i18n): add language detection and sidebar selector to app"
```

---

## Task 5: Replace All Hardcoded Strings in `app.py`

**Files:**
- Modify: `interfaces/streamlit/app.py`

Replace every hardcoded Chinese string with `t("key")`. The complete updated file is shown below — replace the entire file content.

- [ ] **Step 1: Replace entire `app.py` content**

```python
import dataclasses
import streamlit as st
from adapters.poke_api_repository import PokeApiRepository
from adapters.csv_name_provider import CsvNameProvider
from application.calculator import StatCalculator
from application.search_service import SearchService
from application.speed_service import SpeedService
from application.survival_service import AttackInput, SurvivalService
from domain.models.nature import NatureRegistry
from shared.config import CSV_PATH, CACHE_DIR
from shared.i18n.translator import Translator, parse_accept_language


def detect_lang() -> str:
    if "lang" in st.query_params:
        candidate = st.query_params["lang"]
        if candidate in ("zh", "en", "ja"):
            return candidate
    try:
        header = st.context.headers.get("Accept-Language", "")
        return parse_accept_language(header)
    except AttributeError:
        return "zh"


lang = detect_lang()
t = Translator(lang)

st.set_page_config(page_title=t("page_title"), layout="wide")

with st.sidebar:
    chosen = st.selectbox(
        "🌐 Language",
        options=["zh", "en", "ja"],
        format_func=lambda l: {"zh": "繁體中文", "en": "English", "ja": "日本語"}[l],
        index=["zh", "en", "ja"].index(lang),
    )
    if chosen != lang:
        st.query_params["lang"] = chosen
        st.rerun()


@st.cache_resource
def build_services() -> dict:
    repo  = PokeApiRepository(CACHE_DIR)
    csv   = CsvNameProvider(CSV_PATH)
    calc  = StatCalculator()
    return {
        "repo":     repo,
        "search":   SearchService(repo, csv),
        "speed":    SpeedService(calc),
        "survival": SurvivalService(calc),
    }


svc = build_services()

st.title(t("page_title"))

tab_search, tab_speed, tab_survival = st.tabs([t("tab_search"), t("tab_speed"), t("tab_survival")])

# ── Search Tab ──────────────────────────────────────────────────────────────
with tab_search:
    st.header(t("search_header"))
    query = st.text_input(t("search_input_label"), placeholder=t("search_placeholder"))

    if query:
        with st.spinner(t("search_spinner")):
            results = svc["search"].search(query)

        if not results:
            st.warning(t("search_not_found"))
        else:
            for p in results:
                col_img, col_info = st.columns([1, 3])
                with col_img:
                    if p.sprite_url:
                        st.image(p.sprite_url, width=160)
                with col_info:
                    st.subheader(f"{p.name_zh}　{p.name_en.title()}　{p.name_ja}")
                    st.caption(t("search_types_label") + " / ".join(t.type_name(tp) for tp in p.types))
                    b = p.base_stats
                    st.table({
                        t("stat_col_name"): t.strings("stat_names"),
                        t("stat_col_value"): [b.hp, b.attack, b.defense, b.sp_attack, b.sp_defense, b.speed],
                    })

# ── Speed Tab ────────────────────────────────────────────────────────────────
with tab_speed:
    st.header(t("speed_header"))
    st.caption(t("speed_caption"))

    col1, col2 = st.columns(2)

    with col1:
        st.subheader(t("speed_my_mon"))
        my_query = st.text_input(t("speed_name_label"), key="speed_my", placeholder="Garchomp / 烈咬陸鯊")
        my_nature_name = st.selectbox(
            t("speed_nature_label"),
            options=["Hardy", "Timid", "Jolly", "Hasty", "Naive",
                     "Brave", "Relaxed", "Quiet", "Sassy", t("speed_nature_other")],
            key="speed_my_nature",
        )
        if my_nature_name == t("speed_nature_other"):
            my_nature_name = st.text_input(t("speed_nature_input"), key="speed_my_nature_input")

    with col2:
        st.subheader(t("speed_tgt_mon"))
        tgt_query = st.text_input(t("speed_name_label"), key="speed_tgt", placeholder="Kyogre / 蓋歐卡")
        tgt_nature_name = st.selectbox(
            t("speed_nature_label"),
            options=["Hardy", "Timid", "Jolly", "Hasty", "Naive",
                     "Brave", "Relaxed", "Quiet", "Sassy", t("speed_nature_other")],
            key="speed_tgt_nature",
        )
        if tgt_nature_name == t("speed_nature_other"):
            tgt_nature_name = st.text_input(t("speed_nature_input"), key="speed_tgt_nature_input")

    if st.button(t("speed_button"), key="speed_calc") and my_query and tgt_query:
        try:
            my_nature  = NatureRegistry.get_by_name(my_nature_name)
            tgt_nature = NatureRegistry.get_by_name(tgt_nature_name)
        except ValueError as e:
            st.error(t("nature_invalid").format(error=e))
            st.stop()

        with st.spinner(t("speed_spinner")):
            my_results  = svc["search"].search(my_query)
            tgt_results = svc["search"].search(tgt_query)

        if not my_results:
            st.error(t("speed_my_not_found").format(name=my_query))
        elif not tgt_results:
            st.error(t("speed_tgt_not_found").format(name=tgt_query))
        else:
            my_mon  = dataclasses.replace(my_results[0],  nature=my_nature)
            tgt_mon = dataclasses.replace(tgt_results[0], nature=tgt_nature)
            result  = svc["speed"].min_sp_to_outspeed(my_mon, tgt_mon)

            st.divider()
            if result is None:
                st.error(t("speed_cannot_outspeed").format(my=my_mon.name_zh, tgt=tgt_mon.name_zh))
            else:
                st.success(t("speed_success").format(sp=result.sp_needed))
                cols = st.columns(3)
                cols[0].metric(t("speed_metric_sp"), result.sp_needed)
                cols[1].metric(t("speed_metric_speed").format(name=my_mon.name_zh), result.my_speed)
                cols[2].metric(t("speed_metric_speed").format(name=tgt_mon.name_zh), result.target_speed)

# ── Survival Tab ─────────────────────────────────────────────────────────────
with tab_survival:
    st.header(t("surv_header"))
    st.caption(t("surv_caption"))

    col_mon, col_atk = st.columns(2)

    with col_mon:
        st.subheader(t("surv_my_mon"))
        surv_query = st.text_input(t("surv_name_label"), key="surv_mon", placeholder="Garchomp / 烈咬陸鯊")
        surv_nature_name = st.selectbox(
            t("surv_nature_label"),
            options=["Hardy", "Bold", "Impish", "Relaxed", "Lax", t("surv_nature_other")],
            key="surv_nature",
        )
        if surv_nature_name == t("surv_nature_other"):
            surv_nature_name = st.text_input(t("surv_nature_input"), key="surv_nature_input")

    with col_atk:
        st.subheader(t("surv_atk_params"))
        power        = st.number_input(t("surv_power_label"), min_value=1, max_value=250, value=120, key="surv_power")
        attacker_atk = st.number_input(t("surv_atk_label"), min_value=1, max_value=999, value=200, key="surv_atk")
        is_physical  = st.radio(t("surv_cat_label"), [t("surv_cat_physical"), t("surv_cat_special")], key="surv_cat") == t("surv_cat_physical")
        type_mult    = st.select_slider(
            t("surv_mult_label"),
            options=[0.25, 0.5, 1.0, 2.0, 4.0],
            value=1.0,
            key="surv_mult",
        )

    if st.button(t("surv_button"), key="surv_calc") and surv_query:
        try:
            surv_nature = NatureRegistry.get_by_name(surv_nature_name)
        except ValueError as e:
            st.error(t("nature_invalid").format(error=e))
            st.stop()

        with st.spinner(t("surv_spinner")):
            surv_results = svc["search"].search(surv_query)

        if not surv_results:
            st.error(t("surv_not_found").format(name=surv_query))
        else:
            mon = dataclasses.replace(surv_results[0], nature=surv_nature)
            attack = AttackInput(
                power=power,
                attacker_atk=attacker_atk,
                is_physical=is_physical,
                type_multiplier=type_mult,
            )
            prefer_hp, prefer_def = svc["survival"].optimize(mon, attack)

            st.divider()

            # optimize() returns survived=False for both results when impossible;
            # both flags are identical by service contract.
            if not prefer_hp.survived:
                st.error(t("surv_impossible"))
            else:
                st.success(t("surv_success").format(sp=prefer_hp.total_sp))

                col_a, col_b = st.columns(2)

                with col_a:
                    st.subheader(t("surv_plan_hp"))
                    st.metric(t("surv_metric_sp_hp"), prefer_hp.sp_hp)
                    st.metric(t("surv_metric_sp_def"), prefer_hp.sp_def)
                    st.metric(t("surv_metric_final_hp"), prefer_hp.final_hp)
                    st.metric(t("surv_metric_final_def"), prefer_hp.final_def)
                    st.caption(t("surv_metric_total").format(sp=prefer_hp.total_sp))

                with col_b:
                    st.subheader(t("surv_plan_def"))
                    st.metric(t("surv_metric_sp_hp"), prefer_def.sp_hp)
                    st.metric(t("surv_metric_sp_def"), prefer_def.sp_def)
                    st.metric(t("surv_metric_final_hp"), prefer_def.final_hp)
                    st.metric(t("surv_metric_final_def"), prefer_def.final_def)
                    st.caption(t("surv_metric_total").format(sp=prefer_def.total_sp))
```

- [ ] **Step 2: Verify syntax**

```bash
python3 -c "import ast; ast.parse(open('interfaces/streamlit/app.py').read()); print('syntax OK')"
```

Expected: `syntax OK`

- [ ] **Step 3: Run full test suite**

```bash
python3 -m pytest -q
```

Expected: All 79 tests pass.

- [ ] **Step 4: Manual verification checklist**

Start the app:
```bash
streamlit run interfaces/streamlit/app.py
```

Verify in browser:
1. Sidebar shows 🌐 Language selectbox with 繁體中文 selected by default
2. Select **English** → URL changes to `?lang=en`, all UI text switches to English
3. Reload `http://localhost:8501/?lang=en` → remains in English
4. Select **日本語** → URL changes to `?lang=ja`, all UI text switches to Japanese
5. Search tab → type `Garchomp` → type names show in active language (e.g. 龍/Dragon/ドラゴン)
6. Search tab → type `Xyzzy` → not-found message in active language
7. Speed tab → all labels, button, and result text in active language
8. Survival tab → all labels, category radio options, button, result text in active language

- [ ] **Step 5: Final commit**

```bash
git add interfaces/streamlit/app.py
git commit -m "feat(i18n): replace all hardcoded strings with t() translations"
```

---

## Summary

| Task | Files | What it delivers |
|---|---|---|
| 1 | `shared/config.py`, `shared/i18n/__init__.py`, `tests/shared/__init__.py` | I18N_DIR path + package scaffolding |
| 2 | `shared/i18n/translator.py`, 2 test files | Translator class + parse_accept_language + 18 tests |
| 3 | `zh.json`, `en.json`, `ja.json` | All 50 string keys × 3 languages + 18 type names |
| 4 | `app.py` | Language detection + sidebar (strings still zh) |
| 5 | `app.py` | All strings replaced with t() — feature complete |
